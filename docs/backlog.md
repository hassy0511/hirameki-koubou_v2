# 残課題(このページで管理する)

最終更新: 2026-08-03

このアプリの残課題は このページで一元管理する。着手・完了のたびにここを更新し、
完了したものは「済」へ移す。ビジュアル強化の詳細は `codex_visual_backlog.md` に分離してある。

## 進行中・未着手

### 1. ビジュアル強化(Codexへ発注予定)
- 内容: `docs/codex_visual_backlog.md` の10項目(対象カードの絵、かさくらべの入れ物、導入2ページ目 ほか)
- 状態: 発注リスト作成済み・発注待ち

### 2. 小学2年生算数の移植
- 内容: 旧リポジトリ(hirameki-kobo)に実装済みだった6ライン・66ステージを、v2の出題契約(spec.js)に載せ替えて移植する
- 方針: 旧コードは流用せず、`docs/curriculum_math_g2_overview.md` を正本に v2 の生成器スタイルで書き直す。state はコース別に分離する
- 状態: 未着手

### 3. タイムアタック(ライン完走後の高速点検)
- 内容: 各ライン完走後に開く12問制タイムアタック。旧版に存在、v2未実装
- 前提: 記録の保存形式を先に決める(progress とは別枠)
- 状態: 未着手

### 4. ストーリーの深化
- 内容: 現在は初回導入3画面+ライン初回1画面のみ。ライン完走時の「そうち復活」演出、全66ステージ完走の結末、ステージ固有の工房変化(旧版の全体ストーリー相当)が未実装
- 正本: `docs/story_world_and_g1.md`
- 状態: 未着手(結果画面の一言のみ実装済み)

### 5. PWAのオフライン対応
- 内容: sw.js(サービスワーカー)が無く、オフラインで開けない。manifest.json は設置済み
- 状態: 未着手

### 6. 小学3年生算数
- 内容: 7ライン・77ステージの学習設計は `docs/curriculum_math_g3_overview.md` にあり。実装は G2 移植後
- 状態: 設計のみ

### 7. 旧リポジトリの整理
- 内容: hirameki-kobo(旧)をアーカイブ化し、READMEに移転先を明記する
- 状態: 未着手(バックアップは本リポジトリと同期済み)

## 済

- v2骨格の新規実装(G1・6ライン・66ステージ、出題契約テスト先行)
- 実機幅(390px)対応: 全盤面のはみ出し解消+スイープで毎問検査
- 問いと答えの型の不成立を解消(「どうなっている？」廃止、うえ/した・あ/い ラベル)
- 答えの偏り禁止(同一答え3回まで・4種以上・連続4問3種以上)を契約化
- 初回導入3画面+ライン初回導入
- 呼び名と絵の一致(えんぴつ/リボン等の実物形、多義語「のり」除去)
- 学習対象のSVGアイコン化(くだもの・どんぐり・ボタン等、えグラフ含む)
- おはなし盤面のトレイ化(はじめ3こ/つかった2こ)と答え漏れ解消
- iOS文字自動拡大対策
- 管理者モード(URL末尾に `#admin` で全ステージ解放。ホームの「もどす」で解除)

## 運用メモ

- 課題を増やす・消すときは このファイルを更新してコミットする
- 検証はいつも同じ2つ: `node --test tests/quality-contract.test.mjs` と `node tests/browser-sweep.mjs`(要ローカルサーバ)
