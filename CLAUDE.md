# ひらめき工房 v2 での開発ルール

前作(hirameki-kobo)の作り直し。前作で何が失敗し、なぜこの構造になっているかは
`docs/carryover_for_rebuild.md` に全部書いてある。**実装に触る前に必ず読むこと。**

現在の実装状況と残課題は `docs/backlog.md` が正本。**新しい作業に着手する前に必ず確認し、
着手・完了のたびに更新すること。** 個別の発注書がある場合も、完了状態はこの台帳と同期させる。

## 変えないこと

1. 小学生の学習指導要領に沿って、遊びながら算数を学ぶ
2. ステージをクリアすると次のステージが開く
3. 工房の世界観(ルミナ・トト・モクモ、濃紺のフレーム、生成りの紙、木製玩具)

## 構造のルール(前作の失敗から)

- **出題の正しさはテストが定義する。** `tests/` の品質契約がすべての生成問題に対して
  通ること。文章で決めごとを増やすのではなく、検査を増やす
- **既存の生成器の上に補正レイヤーを被せない。** 出題がおかしければ生成器そのものを直す。
  前作は補正レイヤーを被せて2層になり、修正が常に両層をまたいだ
- **生成器は純関数。乱数は必ず引数の rng を使う。** `Math.random` 禁止
- **1ステージ1操作。** 操作を変えてよいのは「読む⇄つくる」の相補ペアだけ
- 盤面は「問題の状態」を見せる。「答えの形」を見せない
- ビルドなし・依存なし。ES modules を index.html から直接読み、テストは `node --test tests/`

## 出題の言葉

- 小1に見せる文は、ひらがな+数字+＋−＝だけ。ヒントと解説も同じ
- 問題文の動詞と操作を一致させる(とろう→取り除く操作、えらぼう→選ぶ操作)
- 場面問題はキャラ文の前置ではなく、数の意味が立ち上がる一文で書く
- 解説は「なぜそうなるか」を式の前に一文置く

## 検証

- `node --test tests/` が全部通ること
- 画面の変更をしたら `node tests/browser-sweep.mjs`(要ローカルサーバ)で全ステージを
  実際に描画し、盤面が表示されること・consoleエラーが無いことを確認する
